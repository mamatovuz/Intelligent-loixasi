[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/public/app-config"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
